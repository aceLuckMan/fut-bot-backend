1. install node.exe
2. install mongodb.exe
3. add environment variables
    - find mongodb path such as "C:\Program Files\MongoDB\Server\6.0\bin" and copy path
    - add mongodb path to environment variables system path
4. find "localhost" in "/dist/assets/index.d73a09a3.js" and replace it to your IP address or domain    name
5. start "start.bat" file 